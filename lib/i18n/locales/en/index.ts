/**
 * English locale exports
 */

import { common } from './common';
import { auth } from './auth';
import { tournament } from './tournament';
import { pages } from './pages';

export default {
  common,
  auth,
  tournament,
  pages,
};

export { common, auth, tournament, pages };